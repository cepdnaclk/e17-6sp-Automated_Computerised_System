import React from "react";
import UnloadedForm from "./forms/UnloadedForm";

function Unloaded() {
  return (
    <React.Fragment>
        <UnloadedForm/>
    </React.Fragment>
  );
}

export default Unloaded;
